# =================================================
# INSTALLATION ORDER IS CRITICAL - FOLLOW SEQUENCE
# =================================================

# STEP 1: Install PyTorch with CUDA 11.8 FIRST
# ⚠️ IMPORTANT: Do NOT install torch via pip! Use conda command:
# conda install pytorch torchvision torchaudio pytorch-cuda=11.8 -c pytorch -c nvidia
#
# This requirements.txt excludes torch packages because they MUST be installed via conda
# for proper CUDA 11.8 integration. pip installation of torch often causes CUDA issues.
#
# If conda is absolutely not available, use this pip command as last resort:
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118

# STEP 2: Core dependencies (install these after PyTorch)
numpy==1.24.3
pandas==2.0.3
scikit-learn==1.3.2

# STEP 3: Core Framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
streamlit==1.28.1

# STEP 4: AI & ML (install after PyTorch and numpy)
openai==1.3.6
sentence-transformers==2.2.2

# STEP 5: Database & Storage
pymongo==4.6.0
chromadb==0.4.18

# STEP 6: Image Processing (install after PyTorch)
opencv-python==4.8.1.78
Pillow==10.1.0
easyocr==1.7.0

# STEP 7: PDF Processing
PyMuPDF==1.23.8
PyPDF2==3.0.1
pytesseract==0.3.10

# STEP 8: Web & HTTP
requests==2.31.0
aiofiles==23.2.1

# STEP 9: Utilities
python-dotenv==1.0.0
pydantic==2.5.0
python-multipart==0.0.6
pytz==2023.3

# STEP 10: Development & Testing
pytest==7.4.3
pytest-asyncio==0.21.1

# =================================================
# SYSTEM DEPENDENCIES (install separately)
# =================================================
# tesseract-ocr (system package)
# poppler-utils (system package)
# CUDA 11.8 (for GPU acceleration)

# =================================================
# IMPORTANT NOTES:
# =================================================
# 1. PyTorch MUST be installed first with conda for CUDA support
# 2. If conda is not available, use pip with CUDA index:
#    pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
# 3. EasyOCR requires PyTorch to be installed first
# 4. Some packages may need to be installed individually if conflicts occur